version: "3.9"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agroforms-frontend
    ports:
      - "8080:80"  # Map Vite preview's internal port to localhost
    restart: unless-stopped  # Automatically restart unless stopped manually