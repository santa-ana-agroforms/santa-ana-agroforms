version: "3.9"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agroforms-frontend-vite
    ports:
      - "5173:4173"  # Map Vite preview's internal port to localhost
    restart: unless-stopped  # Automatically restart unless stopped manually
